{
  "name": "candidates",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^15.5.4",
    "react-dom": "^15.5.4"
  },
  "devDependencies": {
    "concurrently": "^3.4.0",
    "react-scripts": "1.0.5"
  },
  "scripts": {
    "start": "concurrently 'react-scripts start' 'cd server && npm start'",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "bootstrap": "yarn install && (cd server && yarn install)"
  },
  "proxy": "http://localhost:3001",
  "main": "index.js",
  "license": "MIT",
  "eslintConfig": {
    "extends": "react-app"
  }
}
